@model List<DersBlogSite.Models.Makale>

@{
    Layout = "_Layout";
}
<div class="container-fluid ">


    <div class="row">
        @foreach (var makale in ViewBag.mahir)

        {

            <div class="col-sm-6">
                <div class="card mt-4">
                    <div class="card-body">
                        <h5 class="card-title">@makale.Header</h5>
                        <p class="card-text">
                            @makale.Content
                        </p>
                        <a href="#" class="btn btn-primary">Devamını oku..</a>
                    </div>
                </div>
            </div>


            @*<div class="row">
                @foreach (var makale in Model)

                {

                    <div class="col-sm-6">
                        <div class="card mt-4">
                            <div class="card-body">
                                <h5 class="card-title">@makale.Header</h5>
                                <p class="card-text">
                                    @makale.Content
                                </p>
                                <a href="#" class="btn btn-primary">Devamını oku..</a>
                            </div>
                        </div>
                    </div>*@
            @*<div class="col-sm-6">
                    <div class="card mt-4">
                        <div class="card-body">
                            <h5 class="card-title">@makale.Header</h5>
                            <p class="card-text">
                                @makale.Content
                            </p>
                            <a href="#" class="btn btn-primary">Devamını oku..</a>
                        </div>
                    </div>
                </div>*@


        }

    </div>

</div>

