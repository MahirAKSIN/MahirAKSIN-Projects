 import React, { Component } from "react";


const Navbar = (props) => {
  return (
    <div>

      <h3>{props.title}</h3>
      {/* <h2>{props.title.id}</h2> */}
    </div>
  );
};
export default Navbar;
